# Arriba + Nextflow (conda). STAR-Fusion from GitHub — see install_fusion_tools.sh.
name: neoag-fusion
channels:
  - conda-forge
  - bioconda
dependencies:
  - arriba=2.5.1
  - nextflow=24.10.1
  - git
  - curl
  - wget
