# GATK4 + OpenJDK for WES SNV Mutect2 (separate from neoag-tools to avoid Java conflicts).
name: neoag-gatk
channels:
  - bioconda
  - conda-forge
dependencies:
  - gatk4=4.6.2
  - openjdk=17
  - samtools
  - bwa
