# NeoAg Event Pipeline v04 Evidence/Safety/Escape Release - 2026-06-16

## Status

Development release candidate for internal reproducibility testing.

## Main additions

- Standard evidence/provenance sidecars for expression, RNA junction, safety, and tool provenance.
- Peptide safety gate with reference-proteome, normal ligandome, normal-junction, and anchor-risk support.
- Immune escape evidence layer for HLA LOH, B2M/JAK/APM/MHC-II related resistance-risk hypotheses.
- Ranked peptide output now carries escape-related fields including `escape_flag`, `escape_reason`, `escape_multiplier`, and `resistance_risk`.
- SV/WES Phase 1.5 adapter/profile support.
- V04 tests and documentation added.

## Verification

- Unit tests: `.venv/bin/python -m pytest -q` -> `80 passed`.
- End-to-end fixture demo should produce `parsed/`, `presentation/`, `appm/`, `safety/`, `immune_escape/`, `scoring/`, and `reports/` outputs.

## Known environment notes

- `RELEASE.md` in this checkout is root-owned; server refresh notes are kept in `RELEASE_REFRESH_20260615.md`.
- `dist/` is root-owned; v04 release archives are written to `work/releases/`.
- Some external tools may be optional or absent from PATH depending on workflow mode. Current release tests rely on fixtures and Python-layer integration, not full external production runs.

## Non-clinical boundary

This release is for computational research and reproducibility testing only. Safety and immune escape fields are evidence annotations and validation-prioritization aids, not clinical resistance diagnoses or treatment recommendations.

## Server Verification - 2026-06-16

- Unit test command: `.venv/bin/python -m pytest -q`
- Unit test result: `80 passed`
- Basic demo command: `bin/neoag-v03 run-demo --sample-id V04DEMO --profile default --outdir work/release_demo_v04_basic`
- Fixture run command: `bin/neoag-v03 run-v03 --sample-id V04FIX --profile default --outdir work/release_demo_v04_fixture ...`
- Both demo runs produced non-empty parsed, presentation, APPM, safety, immune_escape, scoring, validation_plan, report, and provenance outputs.
- `ranked_peptides.v03.tsv` contains v04 fields: `escape_flag`, `resistance_risk`, `escape_reason`, `escape_multiplier`, and `safety_status`.
- Lightweight release archive is generated under `work/releases/`; `release_manifest.json` is kept outside the archive as the authoritative hash manifest.

## WES SV Profile-driven Priority Caps

- WES SV Phase 1.5 priority caps are now read from `[wes_confidence_caps]` in the active profile.
- Default/fallback mapping remains: `WES_Tier1 -> B`, `WES_Tier2 -> B_CAUTION`, `WES_Tier3 -> C`, `WES_UNINTERPRETABLE -> D`.
- Added a regression test proving a custom profile override changes the emitted `priority_cap`.
- Unit test result after this change: `.venv/bin/python -m pytest -q` -> `81 passed`.

## V0.4.1 APPM2/CCF2/Immune Escape2 Merge - 2026-06-16

- Merged `neoag_v041_appm2_ccf2_immune_escape2_patch_only_20260616.tar`.
- Added APPM 2.0 compatibility layer via `appm_v2.py`, with legacy `build_appm_lite` still producing `appm_lite.tsv` and `appm_summary.tsv`.
- Added CCF 2.0 compatibility layer via `ccf_v2.py`, with richer CCF confidence, interval, clonality, and warning fields in `ccf_lite.tsv`.
- Updated immune escape output to include therapy context and clearer evidence completeness/interpretation fields.
- Updated package version to `0.4.1`.
- Verification: `.venv/bin/python -m pytest -q` -> `84 passed`.
- Fixture `run-v03` completed and produced APPM, CCF, immune escape, scoring, report, and provenance outputs.

## APPM 2.0 Evidence-layer Optimization - 2026-06-18

- Added APPM2 native sidecars: `appm_module_scores.tsv`, `appm_evidence_completeness.tsv`, `appm_conflicts.tsv`, and `appm_peptide_modifiers.tsv`.
- `appm_peptide_modifiers.tsv` is now passed into `score_v03`; ranked peptides carry `appm_multiplier_reason`, `appm_integrity_status`, `appm_evidence_completeness`, and `appm_review_required`.
- HLA allele LOH is recorded in APPM peptide modifiers as review context and left to the immune-escape peptide layer for multiplier/cap handling, reducing double-penalty risk.
- Restored NetMHCpan evidence provenance fields after v0.4.1 patch overlay.
- Manifest pytest now runs under `conf/tools.env.sh` environment.
- Verification with sourced tool environment: `.venv/bin/python -m pytest -q` -> `127 passed`.

## Cross-module Consistency Tests - 2026-06-18

- Added `tests/test_appm_escape_consistency.py` covering APPM -> immune_escape -> scoring consistency.
- Covered B2M biallelic loss: APPM MHC-I defective, immune escape MHC-I high risk, ranked peptide capped to D.
- Covered JAK/IFNG defect: APPM IFNG defective, immune escape IFNG high risk, ranked peptide receives caution/cap context.
- Covered CIITA/MHC-II defect: APPM MHC-II defective and immune escape MHC-II medium risk without penalizing MHC-I peptides.
- Covered no APPM input: APPM and immune escape remain unassessed/inconclusive and ranked peptides are not APPM-penalized.
- Main pipeline now passes generated APPM gene/pathway sidecars into immune escape to avoid divergent APPM re-resolution.
- Verification with sourced tool environment: `.venv/bin/python -m pytest -q` -> `131 passed`.

